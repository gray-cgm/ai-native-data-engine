# Python Services Placeholder

This package space is reserved for future application service modules such as:

- query services
- export services
- scheduler services

Current stage:
- workflow orchestration remains in `python/workflows`
- domain models and contracts remain in `python/core`
- concrete implementations remain in `python/adapters`

This placeholder keeps the repository structure aligned with the documented architecture while avoiding premature implementation.
