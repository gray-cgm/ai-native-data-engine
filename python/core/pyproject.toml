[project]
name = "ad-core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.8.2"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/core"]
