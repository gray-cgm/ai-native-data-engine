# Scheduler Placeholder

This directory is reserved for a future standalone scheduler service.

Current stage:
- orchestration remains in `apps/orchestrator`
- reusable scheduling/orchestration logic remains in `python/workflows`

This placeholder exists to keep the repository structure aligned with the documented FDL-inspired architecture without implying that a runnable scheduler service already exists.
