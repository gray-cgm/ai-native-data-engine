[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.6",
  "python-dotenv>=1.0.1",
  "ad-core",
  "ad-adapters",
  "ad-workflows",
  "ad-profiles"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
